import React from "react";
import ReactDataGrid from "../components/results"

const Results = () => {
    return (
        <div>
            <h1>
                RESULTS
            </h1>
            <ReactDataGrid/>
        </div>
    );
};

export default Results;